import {
    All,
    AutoInject,
    Lazy,
    NewInstance,
    Optional,
    Parent,
    Registration,
    Resolve,
    Singleton,
    Transient,
} from './decorators';

import { install } from './plugin';

export default {
    install,
    All,
    AutoInject,
    Lazy,
    NewInstance,
    Optional,
    Parent,
    Registration,
    Resolve,
    Singleton,
    Transient,
};
