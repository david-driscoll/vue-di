import {
    All,
    AutoInject,
    Builder,
    Container,
    Factory,
    Lazy,
    NewInstance,
    Optional,
    Parent,
    Resolve,
    Singleton,
    Transient,
} from './index';

import { InjectModule, InjectVuexModule } from './InjectModule';
export * from './index';

export { InjectModule, InjectVuexModule };

export default {
    All,
    AutoInject,
    Builder,
    Container,
    Factory,
    Lazy,
    NewInstance,
    Optional,
    Parent,
    Resolve,
    Singleton,
    Transient,
    InjectModule,
    InjectVuexModule,
};
