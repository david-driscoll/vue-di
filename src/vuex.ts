import {
    All,
    AutoInject,
    Builder,
    Container,
    Factory,
    Lazy,
    NewInstance,
    Optional,
    Parent,
    Resolve,
    Singleton,
    Transient,
} from './index';

import { InjectModule } from './InjectModule';
export * from './index';

export { InjectModule };

export default {
    All,
    AutoInject,
    Builder,
    Container,
    Factory,
    Lazy,
    NewInstance,
    Optional,
    Parent,
    Resolve,
    Singleton,
    Transient,
    InjectModule,
};
